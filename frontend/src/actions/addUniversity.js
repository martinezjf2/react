


export const addUniversity = (data) => {
    
     // why return dispath, then we can import it to the input file and then connect it to the redux store, will be calling a function dispatching something to the reducer.

    return (dispatch)  = {

    }
   
}