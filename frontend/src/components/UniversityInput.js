import React, { Component } from "react";

// controlled form to control the values, because of local state holding the values

export default class UniversityInput extends Component {
    

    render() {
        return (
            <div>
             UniversitiesInput
            </div>
        )
    }

}